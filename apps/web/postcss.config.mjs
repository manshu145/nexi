// Tailwind CSS v4 uses a PostCSS plugin (or the Vite plugin -- we use
// PostCSS here because Next.js plugs into PostCSS natively).
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
